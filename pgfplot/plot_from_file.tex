\documentclass{article}
\usepackage{pgfplots}
\pgfplotsset{compat=1.8}

\begin{document}

\begin{tikzpicture} % plotting with data in columns
  \begin{axis}[
      xlabel=Here is x,
      ylabel=Here is y,
      legend style={
        at={(0.5,-0.18)},       % 0.5 times xmin,xmax and -0.18 times ymin,ymax, think affine comb.
        anchor=north,
        legend columns=-1       % no columns, only rows
      },
    ]
    \addplot table [x=X, y=gumbel] {data.dat};
    \addplot table [x=X, y=gaussian] {data.dat};
    \legend{gumbel,gaussian}
  \end{axis}
\end{tikzpicture}


\begin{tikzpicture} % plotting with data in columns
  \begin{axis}[
      xlabel=Here is x,
      ylabel=Here is y,
      legend style={
        at={(0.5,-0.18)},       % 0.5 times xmin,xmax and -0.18 times ymin,ymax, think affine comb.
        anchor=north,
        legend columns=-1       % no columns, only rows
      },
    ]
    \addplot table [x=a, y=b, col sep=comma] {../data/data.csv}; % addplot table reads table from file
    \addplot table [x=a, y=c, col sep=comma] {../data/data.csv};
    \addplot table [x=a, y=d, col sep=comma] {../data/data.csv};
    \legend{first,second,third}
  \end{axis}
\end{tikzpicture}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{tikzpicture}
  \begin{axis}[
      ybar,                     % bars along y
      bar width=2pt
    ]
    \addplot table[x=rounds, y=RRS] {../data/table.dat};
    \addplot table[x=rounds, y=QRC] {../data/table.dat};
  \end{axis}
\end{tikzpicture}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{tikzpicture}
  \begin{loglogaxis}[
      xlabel=Cost,
      ylabel=Error
    ]
    \addplot[color=red,mark=x]
    coordinates {
      (5,    8.31160034e-02)
      (17,   2.54685628e-02)
      (49,   7.40715288e-03)
      (129,  2.10192154e-03)
      (321,  5.87352989e-04)
      (769,  1.62269942e-04)
      (1793, 4.44248889e-05)
      (4097, 1.20714122e-05)
      (9217, 3.26101452e-06)
    };
    \addplot[color=blue,mark=*]
    table[x=Cost,y=Error] {../data/costerror.dat};
    \legend{Case 1,Case 2}
  \end{loglogaxis}
\end{tikzpicture}
\end{document}
